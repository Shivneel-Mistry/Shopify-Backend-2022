<%= form_with(model: warehouse) do |form| %>
  <% if warehouse.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(warehouse.errors.count, "error") %> prohibited this warehouse from being saved:</h2>

      <ul>
        <% warehouse.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="d-flex flex-row justify-content-center">
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <p class="card-text">
      <div>
    <%= form.label :warehouse, style: "display: block" %>
    <%= form.text_field :warehouse %>
  </div><br>
  <div>
    <%= form.submit "Create Warehouse", class:"btn btn-secondary" %> 
  </div><br>
  <div>
    <%= link_to "Back to warehouses", warehouses_path, class:"btn btn-outline-secondary"%>
  </div>
    </p>  
  </div>
</div>
</div>
  
<% end %>
